server.port=${PORT}
server.address=0.0.0.0

spring.application.name=fpa

spring.datasource.url=jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}?escapeSyntaxCallMode=callIfNoReturn
spring.datasource.username=${PGUSER}
spring.datasource.password=${PGPASSWORD}
spring.datasource.hikari.maximum-pool-size=3

server.tomcat.threads.min-spare=3
server.tomcat.threads.max=5

spring.jackson.default-property-inclusion=non_null

application.currency.sync-api.url=https://openexchangerates.org/api/latest.json?app_id={app_id}
application.currency.sync-api.app-id=8be0b27399a84c53ab47e8cd1df22bd2
